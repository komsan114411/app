{
  "name": "myapp-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor shell that wraps the PWA as a native Android/iOS app",
  "scripts": {
    "sync": "cap sync",
    "copy": "cap copy",
    "android": "cap open android",
    "ios": "cap open ios",
    "build:debug": "cap sync android && cd android && gradlew.bat assembleDebug",
    "build:release": "cap sync android && cd android && gradlew.bat assembleRelease",
    "postinstall": "node scripts/prepare-web.js"
  },
  "dependencies": {
    "@capacitor/android": "6.2.0",
    "@capacitor/app": "6.0.2",
    "@capacitor/core": "6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "6.2.0"
  }
}
